import { LineThroughDirective } from './line-through.directive';

describe('LineThroughDirective', () => {
  it('should create an instance', () => {
    const directive = new LineThroughDirective();
    expect(directive).toBeTruthy();
  });
});
